# Copy this file to android/keystore.properties and fill with your keystore values.
# Do NOT commit the actual keystore.properties with real passwords to version control.

storeFile=./my-release-key.jks
storePassword=your_keystore_password
keyAlias=my_key
keyPassword=your_key_password
