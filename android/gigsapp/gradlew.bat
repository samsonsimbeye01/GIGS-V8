@echo off
echo This project includes gradle-wrapper properties but no wrapper scripts.
echo If you have Gradle installed, run: gradle wrapper
echo Or generate wrapper files on another machine/CI and commit them.
exit /b 1
